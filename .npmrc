# engine-strict matches the cfg-* convention (Node 22+).
engine-strict=true

# @crit-fumble scope is published to GitHub Packages.
# CI sets NODE_AUTH_TOKEN from secrets.GITHUB_TOKEN.
# Local installs use `gh auth token` — see README setup.
@crit-fumble:registry=https://npm.pkg.github.com
